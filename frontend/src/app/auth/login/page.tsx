"use client";

import { LoginForm } from "@/components/auth/login-form";

const LoginPage = () => {
  return (
    <div className="h-full">
      <LoginForm />
    </div>
  );
};

export default LoginPage;
